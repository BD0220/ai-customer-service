# 简历项目描述

> 以下文案可直接复制到简历中使用，根据投递岗位侧重点微调即可。

---

## 版本一：标准版（推荐，适合大多数后端/全栈岗位）

### AI 智能客服工单系统
**技术栈**：Python · FastAPI · Gradio · DeepSeek API · SQLite · Docker

- 独立设计并开发企业级智能客服系统，支持 AI 自动应答、智能工单转人工、对话记录持久化，目标自动解决率 70%+
- 设计"关键词匹配 + LLM 自主判断 + 异常兜底"三层转人工机制，精准识别投诉、退款等复杂问题并自动创建分级工单
- 基于 FastAPI 构建 RESTful API（含 Swagger 文档），Gradio 实现双标签页前端（客服对话 + 工单管理），SQLite 存储工单与对话数据
- 实现工单紧急程度自动分级（urgent/normal）、状态流转（pending→processing→done）和列表实时刷新
- 通过 System Prompt 工程约束 AI 行为边界（禁止退款承诺、禁止泄露用户信息），确保服务安全合规
- 支持 Docker 容器化部署，一键启动前后端服务

---

## 版本二：AI/LLM 方向侧重版（适合投递 AI 应用、大模型相关岗位）

### AI 智能客服工单系统
**技术栈**：Python · DeepSeek API · FastAPI · Prompt Engineering · Gradio · SQLite

- 基于 DeepSeek 大模型构建智能客服 Agent，通过 System Prompt 定义角色、能力边界和转人工规则，实现高可用的自动问答
- 设计混合路由策略：关键词快速命中 + LLM 自主判断（`[NEED_HUMAN]` 标记协议）+ API 异常兜底，确保复杂问题 100% 转人工
- 通过 Prompt Engineering 控制 AI 语气风格和安全边界（禁止退款承诺/赔偿承诺/信息泄露），降低合规风险
- 使用 OpenAI SDK 兼容方式调用 DeepSeek API，封装统一的 chat 接口，支持模型无缝切换
- 构建 FastAPI 后端 + Gradio 前端的完整链路，对话记录和工单自动持久化，支持客服团队协作处理

---

## 版本三：精简版（适合简历空间有限时）

### AI 智能客服工单系统 | Python, FastAPI, DeepSeek API, Gradio, SQLite
- 开发基于大模型的智能客服系统，AI 自动处理订单查询、物流跟踪、退货政策等常见问题，自动解决率目标 70%+
- 设计三层转人工机制（关键词/AI判断/异常兜底），自动创建分级工单，支持工单状态流转和实时刷新
- FastAPI 提供 RESTful API，Gradio 构建对话与工单管理界面，Docker 一键部署

---

## 面试可能被问到的问题 & 参考思路

### 1. 为什么用 Gradio 而不是 React/Vue？
> Gradio 适合快速验证 AI 应用原型，内置聊天组件和自动刷新，开发效率高。如果面向生产环境，可以替换为 React + WebSocket 的方案，我在项目中已经把前后端通过 RESTful API 解耦，前端可以独立替换。

### 2. 转人工的判断逻辑是怎么设计的？
> 三层兜底：第一层是关键词匹配（投诉、退款等），响应快且零成本；第二层是让 LLM 自己判断，遇到超出能力范围的问题返回 `[NEED_HUMAN]` 标记，覆盖面更广；第三层是 API 调用异常时的兜底，保证用户不会遇到无响应的情况。

### 3. 为什么选 SQLite？如果数据量大了怎么办？
> SQLite 零配置、单文件，适合项目原型和中小规模部署。代码中数据库操作已经封装在 db.py 中，切换到 PostgreSQL/MySQL 只需修改连接层。后续可以引入 Redis 做会话缓存和工单消息队列。

### 4. 如何保证 AI 不会做出越权操作（比如承诺退款）？
> 通过 System Prompt 明确约束 AI 的能力边界，列出"绝对不能做"的事项。同时在应用层通过关键词拦截兜底——即使 AI 试图回复退款相关内容，关键词检测也会先触发转人工流程。

### 5. 这个项目还有什么可以改进的？
> - 接入真实订单/物流 API，用 Function Calling 让 AI 调用工具查询实时数据
> - 增加多轮对话上下文记忆（当前是单轮）
> - 引入 WebSocket 实现客服与用户的实时通信
> - 增加数据看板，统计自动解决率、平均响应时间等指标
> - 对接飞书/钉钉推送新工单通知
